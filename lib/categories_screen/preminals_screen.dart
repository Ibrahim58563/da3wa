import 'package:flutter/material.dart';

class PreliminariesScreen extends StatelessWidget {
  const PreliminariesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
